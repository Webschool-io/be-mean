'use strict';

const MODEL_NAME = "PHOTOS"
		, mongoose = require('mongoose')
		, sckeconst = require('./photos_schema')
		,	Schema = mongoose.Schema
		,	schema = new Schema(skeconston)
		,	Model =  mongoose.model(MODEL_NAME,schema)
		;

module.exports = Model;
